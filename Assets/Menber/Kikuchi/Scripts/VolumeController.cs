using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class VolumeController : MonoBehaviour
{
    /*
    public List<TileObj> volumes = new List<TileObj>();

    void Start()
    {
        GetComponentsInChildren(volumes);
    }

    public void TileChange(float getNum)
    {
        foreach (var tile in volumes)
        {
            if (tile.tileNum <= getNum * 10)
            {
                tile.TileON();
            }
            else
            {
                tile.TileOFF();
            }
        }
    }
    */
}
